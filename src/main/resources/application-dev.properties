# Local development profile — never use in production.
# Activate with: -Dspring.profiles.active=dev  OR  SPRING_PROFILES_ACTIVE=dev

spring.datasource.password=password
spring.security.user.name=admin
spring.security.user.password=admin123

# H2 console only for local debugging (still requires admin credentials)
spring.h2.console.enabled=true
spring.jpa.show-sql=true
logging.level.com.antivirus=DEBUG
